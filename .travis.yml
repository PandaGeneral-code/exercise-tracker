sudo: required
language: generic
services: docker
before_install:
  - docker build -t sashazahnitko/react-test -f Dockerfile.dev ./exercise-tracker-client
script:
  - docker run -e CI=true sashazahnitko/react-test npm test -- -- coverage
